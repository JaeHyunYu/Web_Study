{
    "include": ["src"],
    "compilerOptions": {
        "outDir": "build",
        "target": "ES6",
        "lib": ["ES6","DOM"],
        "strict": true,
        "allowJs": true
        // ES6를 지원하는 환경에서 실행될 것이라 가정하고
        // DOM:브라우저 위에서 실행한다는 의미!
        // 즉 ES6를 지원하는 서버와 DOM, 즉 브라우저 환경에서 코드를 실행시킬거라고 가정
    }
}   


  // 해당 코드에서 우리는 build라는 폴더에 만들어진 코드를 넣을것이라 알려준 것임
 //outDir는 자바스크립트 파일이 생성될 디렉터리를 지정
    //타입스크립트는 컴파일러이니까, 이파일(index.ts)들을 일반적인 자바스크립트로 컴파일 시켜줄 것임
    //따라서 우리는 타입스크립트에게 저 자바스크립트 파일을 어디에 만들지 알려줘야함
//자바스크립트로 컴파일하고 싶은 모든 디렉터리를 넣어줌
    // 타입스크립트가 src의 모든 파일을 확인한다는 것을 의미//target은 어떤 버전의 자바스크립트로 너가 컴파일하고 싶은지를 나타냄
   